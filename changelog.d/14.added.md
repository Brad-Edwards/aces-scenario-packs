### Added

- A capture workflow placement decision (`docs/capture-workflow-placement.md`)
  and ADR 0004 that resolve the capture/inventory ownership review deferred by
  ADR 0003: capture work is split by responsibility — ACES-semantic capture
  stays in ACES core, pack-authoring capture support is adopted here only on a
  linked issue, and runtime capture stays downstream — with the ACES and APTL
  follow-ups linked and a guardrail that no capture asset moves before its
  placement is recorded. A structural test keeps the decision machine-checkable.
