# Lineage

**Optional.** Where the content and ideas came from — real-world incidents,
prior scenarios, source material, and any reuse — so the scenario's provenance
is traceable.

This is human prose; the machine-readable source/licensing/safety/publication
record is `docs/provenance-ledger.yaml`. Pack trust (integrity/authenticity) is
owned upstream by the ACES reusable-asset trust policy (ACES ADR-071); see
[ADR 0010](../../../../docs/decisions/adrs/0010-consume-aces-reusable-asset-trust-policy.md).
