# Schemas

This directory will hold scenario-pack schemas after the schema migration issue
is implemented.

Expected future schema families include pack metadata, compatibility manifests,
provenance ledgers, artifact boundaries, runtime profiles, delivery bundles,
lifecycle metadata, validation metadata, and release metadata.
